# 备份规范

## 备份目录结构

```
bak/
├── source/              完整源代码归档 (.zip)
├── dev-tools/           开发工具集 (安装指引/脚本)
├── patches/             运行补丁集 (WebView2/字体/注册表)
├── publish/             发布产物 (自包含 .exe)
└── (本文件归档于 docs/BACKUP.md)
```

## 各目录说明

### `source/` — 完整源代码归档

存放项目全部源代码的压缩包，包括但不限于：

- Kotlin/Java 源文件
- Gradle 构建脚本 (`build.gradle.kts`, `settings.gradle.kts`)
- Android 资源文件 (`res/`, `AndroidManifest.xml`)
- ProGuard 混淆规则
- Gradle Wrapper 配置

**归档格式：** `.zip`，命名规则 `BlockRedFruit-src-v{版本号}.zip`

**归档时机：** 每次版本发布前，将当前源码树完整打包存入此目录。

### `dev-tools/` — 开发工具集

存放开发环境搭建所需的工具安装指引与辅助脚本：

- JDK 安装与配置指南
- Android Studio / IntelliJ IDEA 安装指引
- Android SDK 平台工具说明
- Gradle 版本及镜像源配置说明
- 开发环境一键配置脚本（如有）

**内容形式：** 安装指引以 `.md` 或 `.txt` 编写；脚本以 `.sh`/`.bat`/`.ps1` 提供。

### `patches/` — 运行补丁集

存放目标设备运行时所需的补丁与系统组件：

- WebView2 运行时安装包
- 字体补丁/字体文件
- 注册表修复脚本
- 设备特定兼容性补丁

**内容形式：** 补丁以原格式提供，并附带说明文档注明适用场景与安装步骤。

### `publish/` — 发布产物

存放构建输出的可直接部署的产物：

- 自包含可执行文件 (`.exe`)
- 签名后的 APK 文件
- Release 构建输出
- 版本发布说明

**归档格式：** 产物按版本号分目录存放，目录名 `v{版本号}/`。

## 维护规范

1. **备份时机** — 每次公开发布前，必须完成上述四个目录的归档更新。
2. **版本对应** — 所有归档文件名与目录名须包含版本号，确保可追溯。
3. **完整性校验** — 归档完成后应验证压缩包可解压、文件无损坏。
4. **本文件归档** — `BACKUP.md` 本身随源代码一并归档于 `docs/` 目录下。
