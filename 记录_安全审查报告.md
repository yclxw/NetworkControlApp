# 安全审查报告 — BlockRedFruit (红果断网)

**审查日期**：2026-05-17  
**审查范围**：`C:\DStui\BlockRedFruit` 全部源码、配置、构建脚本  
**审查方法**：静态代码分析，逐文件审查  

---

## 一、项目概要

| 项目 | 详情 |
|------|------|
| 应用名称 | 红果断网 (BlockRedFruit) |
| 平台 | Android |
| 语言 | Kotlin |
| 构建系统 | Gradle 8.13 + AGP 8.13.0 |
| 最低 SDK | 24 (Android 7.0) |
| 目标 SDK | 35 (Android 15) |
| 核心 API | Android VpnService |

**功能描述**：扫描设备上所有名称包含「红果」的已安装应用，通过 Android VpnService 创建本地 VPN，利用 `addAllowedApplication` 白名单机制将目标应用的网络流量导入 VPN 后静默丢弃，实现精准断网，不影响其他应用。

**源码文件清单**：

| 文件 | 行数 | 功能 |
|------|------|------|
| [MainActivity.kt](app/src/main/java/com/example/blockredfruit/MainActivity.kt) | 163 | 主界面：VPN 开关、权限请求、目标应用列表 |
| [BlockVpnService.kt](app/src/main/java/com/example/blockredfruit/BlockVpnService.kt) | 219 | VPN 服务核心：白名单路由、数据包丢弃、前台通知 |
| [AppScanner.kt](app/src/main/java/com/example/blockredfruit/AppScanner.kt) | 64 | 扫描已安装应用，筛选名称含「红果」者 |
| [AppListAdapter.kt](app/src/main/java/com/example/blockredfruit/AppListAdapter.kt) | 39 | RecyclerView 适配器，展示目标应用列表 |
| [AndroidManifest.xml](app/src/main/AndroidManifest.xml) | 49 | 清单：权限声明、组件注册 |
| [build.gradle.kts](app/build.gradle.kts) | 50 | 应用构建配置 |
| [settings.gradle.kts](settings.gradle.kts) | 23 | 仓库镜像与模块注册 |
| [proguard-rules.pro](app/proguard-rules.pro) | 7 | 混淆保留规则 |

---

## 二、后门/木马检查

### 检查结论：✅ 未发现后门或木马

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 数据外传 | ✅ 无风险 | VPN 仅读取数据包并丢弃（`FileInputStream.read()` → 无写入），不连接任何外部服务器 |
| 远程命令与控制 (C2) | ✅ 无风险 | 代码中无任何出站网络连接：无 `Socket`、无 `HttpURLConnection`、无 `OkHttp`、无 `WebView` |
| 代码混淆/隐藏 | ✅ 无风险 | 全部为明文可读 Kotlin，无反射加载、无 `DexClassLoader`、无 `Runtime.exec()`、无 JNI |
| 隐藏依赖 | ✅ 无风险 | 依赖全部为官方 AndroidX/Google Material 库，版本号与 Maven Central 一致 |
| 动态代码执行 | ✅ 无风险 | 无 `ClassLoader` 动态加载、无脚本引擎 |
| 可疑后台行为 | ✅ 无风险 | 无 `JobScheduler`、无 `WorkManager`、无隐式广播接收器（仅注册前台可见的 VPN 服务广播） |
| 备份目录差异 | ✅ 无风险 | `bak/` 中源码与 `app/src/` 完全一致（diff 无差异），仅包含历史构建 APK |

### 关键代码行为验证

**VPN 数据包处理**（[BlockVpnService.kt:143-158](app/src/main/java/com/example/blockredfruit/BlockVpnService.kt#L143-L158)）：

```kotlin
private fun startReader() {
    readerThread = Thread({
        val buffer = ByteArray(32767)
        try {
            val input = FileInputStream(vpnInterface!!.fileDescriptor)
            while (!Thread.currentThread().isInterrupted) {
                val len = input.read(buffer)  // 只读不写，数据包被丢弃
                if (len < 0) break
            }
        } catch (_: IOException) { ... }
    }, "VpnReader").apply { isDaemon = true; start() }
}
```

数据包从 TUN 设备读取后直接丢弃，不转发、不记录、不发送到任何地方。这是实现"断网"的正确方式。

**应用扫描逻辑**（[AppScanner.kt:18-33](app/src/main/java/com/example/blockredfruit/AppScanner.kt#L18-L33)）：

仅通过 `PackageManager.getInstalledApplications()` 读取已安装应用列表，按名称匹配关键词「红果」。不访问应用数据、不读取文件、不 Hook 其他进程。

---

## 三、安全风险发现

### 风险 1：第三方 Maven 镜像 — 供应链风险 ⚠️ 中风险

**位置**：[settings.gradle.kts:3-5](settings.gradle.kts#L3-L5)

```kotlin
maven { url = uri("https://maven.aliyun.com/repository/google") }
maven { url = uri("https://maven.aliyun.com/repository/public") }
maven { url = uri("https://maven.aliyun.com/repository/gradle-plugin") }
```

**问题**：使用阿里云 Maven 镜像替代 Google / Maven Central 官方源。`FAIL_ON_PROJECT_REPOS` 模式下，仅镜像仓库可用——若镜像被劫持或投毒，依赖包可能被替换为恶意版本。

**修复建议**：

```kotlin
repositories {
    google()                                                     // 首选官方源
    mavenCentral()
    maven { url = uri("https://maven.aliyun.com/repository/google") }     // 镜像作为 fallback
    maven { url = uri("https://maven.aliyun.com/repository/public") }
}
```

或使用 Gradle 仓库内容过滤，限制镜像仅用于特定 group，并在 CI 中启用依赖校验（checksum verification）。

---

### 风险 2：allowBackup 未关闭 ⚠️ 低风险

**位置**：[AndroidManifest.xml:14](app/src/main/AndroidManifest.xml#L14)

```xml
android:allowBackup="true"
```

**问题**：允许通过 `adb backup` 导出应用数据。虽此应用不存储敏感用户数据，但开启备份增加了不必要的攻击面。

**修复建议**：

```xml
android:allowBackup="false"
```

---

### 风险 3：Release 版本日志未移除 ℹ️ 低风险

**位置**：
- [MainActivity.kt](app/src/main/java/com/example/blockredfruit/MainActivity.kt) — 10 处 `Log.e/w/i` 调用
- [BlockVpnService.kt](app/src/main/java/com/example/blockredfruit/BlockVpnService.kt) — 8 处 `Log.e/w/i/d` 调用

**问题**：Release 版本中日志输出会泄露应用内部行为（扫描到的包名、VPN 启动/停止时间等）到 logcat，任何可读取 logcat 的应用或 ADB 用户均可查看。

**修复建议**：在 [proguard-rules.pro](app/proguard-rules.pro) 中添加：

```proguard
-assumenosideeffects class android.util.Log {
    public static int v(...);
    public static int d(...);
    public static int i(...);
    public static int w(...);
    public static int e(...);
}
```

或使用 Timber 等日志库在 release 版本中植入空实现。

---

### 风险 4：`QUERY_ALL_PACKAGES` 权限 ℹ️ 信息

**位置**：[AndroidManifest.xml:5-6](app/src/main/AndroidManifest.xml#L5-L6)

```xml
<uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
    tools:ignore="QueryAllPackagesPermission" />
```

**说明**：此权限允许扫描设备上所有已安装应用，Google Play 对此权限有严格审核政策。此应用功能确实需要此权限（扫描「红果」应用），属于合理使用，但需注意：

- 上架 Google Play 时需提交权限声明，说明使用理由
- Google Play 可能要求证明此为核心功能而非辅助功能

---

## 四、权限清单与分析

| 权限 | 用途 | 风险 |
|------|------|------|
| `QUERY_ALL_PACKAGES` | 扫描所有已安装应用以匹配「红果」关键词 | 低 — 功能必需 |
| `FOREGROUND_SERVICE` | VPN 前台服务 | 低 — 系统要求 |
| `FOREGROUND_SERVICE_SPECIAL_USE` | Android 14+ 前台服务类型声明 | 低 — 系统要求 |
| `POST_NOTIFICATIONS` | Android 13+ 通知权限 | 低 — 系统要求 |
| `BIND_VPN_SERVICE` | 绑定 VPN 服务 | 低 — 系统自动授予 |

所有权限均为实现 VPN 断网功能的必要权限，无冗余或可疑权限声明。

---

## 五、依赖安全分析

| 依赖 | 版本 | 来源 | 风险 |
|------|------|------|------|
| `androidx.core:core-ktx` | 1.13.1 | Google Maven | ✅ 官方 |
| `androidx.appcompat:appcompat` | 1.7.0 | Google Maven | ✅ 官方 |
| `com.google.android.material:material` | 1.12.0 | Google Maven | ✅ 官方 |
| `androidx.constraintlayout:constraintlayout` | 2.1.4 | Google Maven | ✅ 官方 |
| `androidx.lifecycle:lifecycle-runtime-ktx` | 2.8.3 | Google Maven | ✅ 官方 |
| `androidx.lifecycle:lifecycle-viewmodel-ktx` | 2.8.3 | Google Maven | ✅ 官方 |
| AGP | 8.13.0 | Google Maven | ✅ 官方 |
| Kotlin Plugin | 2.0.0 | Gradle Plugin Portal | ✅ 官方 |

所有依赖均为 Android 官方生态组件，无第三方非官方库。

---

## 六、总体评估

| 维度 | 评级 | 说明 |
|------|------|------|
| 后门/木马 | ✅ 安全 | 未发现任何恶意代码或可疑行为 |
| 隐私泄露 | ✅ 安全 | 不收集、不存储、不传输任何用户数据 |
| 权限使用 | ✅ 合理 | 所有权限均为功能必需，无不必要权限 |
| 代码质量 | ✅ 良好 | 代码结构清晰，职责分离，异常处理到位 |
| 供应链安全 | ⚠️ 中等 | 构建依赖阿里云 Maven 镜像 |
| 配置安全 | ⚠️ 低 | allowBackup 开启、日志未移除 |

### 修复优先级

| 优先级 | 问题 | 修复工作量 |
|--------|------|------------|
| **高** | 将 Google/Maven Central 官方源设为首选仓库 | 1 行配置 |
| **中** | 关闭 `allowBackup` | 1 行配置 |
| **低** | Release 版本移除日志输出 | 5-10 行 ProGuard 规则 |

---

## 七、结论

**BlockRedFruit（红果断网）是一款合法的 Android 工具应用，代码行为与 README 描述完全一致。审查未发现后门、木马、远程控制、数据窃取或任何形式的恶意代码。**

应用的核心机制（VpnService 白名单 + 数据包丢弃）是 Android 平台上实现单应用断网的标准做法，技术实现正确且透明。

建议修复上述 3 个安全配置问题后即可安全使用。
