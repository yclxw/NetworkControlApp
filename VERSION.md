# 版本规范

## 版本号格式

```
v{主版本}.{次版本}.{日期时间}
```

示例：`v1.0.20260524`

## 字段定义

| 字段 | 含义 | 取值规则 |
|------|------|----------|
| **主版本** | 大版本标识 | `2` = 大版本更新（架构重构、平台迁移等）<br>`1` = 正式版（功能稳定、面向用户）<br>`0` = 测试版（开发中、内部测试） |
| **次版本** | 修改计数 | 每修改一次 **+1**，从 `0` 开始 |
| **日期时间** | 构建日期 | `YYYYMMDD` 格式，精确到日 |

## 版本演进规则

### 主版本号升级

- **0 → 1**：首个功能完整的正式发布版本。
- **1 → 2**：发生不兼容的架构变更、平台迁移或重大重构。
- 主版本号不可回退。

### 次版本号递增

- 每次代码修改（新增功能、Bug 修复、重构、文档更新）后递增 `+1`。
- 次版本号随修改持续累加，不因主版本升级而重置。

### 日期时间

- 取构建当天的日期，格式 `YYYYMMDD`。
- 同一天内多次构建的日期部分不变，由次版本号区分。

## 当前版本

**v1.0.20260524**

| 字段 | 值 | 说明 |
|------|-----|------|
| 主版本 | `1` | 正式版 |
| 次版本 | `0` | 初始发布 |
| 日期 | `20260524` | 2026 年 5 月 24 日 |

## 版本号位置

版本号在以下文件中同步维护：

| 文件 | 字段 |
|------|------|
| `app/build.gradle.kts` | `versionCode` / `versionName` |
| `docs/CHANGELOG.md` | 每个版本的标题 |
| `docs/VERSION.md` | 本文件「当前版本」章节 |

## 版本对照

| Android `versionName` | 本项目版本号 | 说明 |
|-----------------------|-------------|------|
| `1.0.0` | `v1.0.20260524` | 初始正式版 |
