# BlockRedFruit 提示词分析总结

> 基于本次开发全过程的回顾，提炼有效和无效的提示词模式，为后续工作提供参考。

---

## 一、有效模式

### 1. 初始需求：命中率 90%

```
开发一个app
启动app后，开启一个vpn，将所有名称包含 红果 的app 接入vpn，实现断网效果
不影响其他app
图标使用红色的禁止圆形标志
```

**有效原因：** 一句话包含四个关键要素：

| 要素 | 说明 |
|------|------|
| 目标对象 | 名称含「红果」的 app |
| 核心手段 | VPN |
| 预期效果 | 断网 |
| 边界约束 | 不影响其他 app |

> 原则：**who + how + what + not**，避免模糊词。

### 2. 迭代指令：命中率 80%

```
生成apk，文件名后面加上日期时间，不要删除以前的版本
将完整源代码和apk复制一份到 C:\DStui\BlockRedFruit\bak
将图标和app的图片换为 C:\DStui\OFF.png
```

**有效原因：** 三个要素齐全：

| 要素 | 说明 |
|------|------|
| 动作 | 复制 / 改名 / 替换 |
| 路径 | 精确到具体目录和文件名 |
| 约束 | 不删除以前版本 |

> 原则：**动词 + 路径 + 约束条件**，不留歧义。

---

## 二、无效 / 可优化模式

### 1. 缺少环境前置说明

**问题：** 初始提示词未指定 JDK / Gradle / Kotlin 版本，导致三次构建失败。

| 轮次 | 错误 | 原因 |
|------|------|------|
| 第 1 次 | `Could not install Gradle distribution` | 官方源超时 |
| 第 2 次 | `Unsupported class file major version 69` | Gradle 8.6 ASM 不支持 Java 25 |
| 第 3 次 | `java.lang.IllegalArgumentException: 25.0.3` | Kotlin 编译器不认识 Java 25 |

**代价：** 约 40% 的对话轮次消耗在环境兼容问题上。

**改进方案：** 项目初始提示词中显式写死：

```
技术栈：Kotlin + AGP 8.4.2 + Gradle 8.13 + JDK 21
→ 后端可一次就绪
```

### 2. 崩溃描述不精确

```
app安装后，点击自动缩小返回了，无法使用
```

**问题：** "自动缩小返回" 可能指 Activity 崩溃回桌面、finish() 被调用、
ANR 被系统杀掉三种情况，三者完全不同的修复方向，AI 只能靠猜。

**改进方案：** 附上日志：

```
adb logcat -s BlockRedFruit:* *:S
→ 一行堆栈胜于十行描述
```

---

## 三、推荐提示词模板

### 初始任务模板

```
开发一个 Android app：
- 技术栈：Kotlin + JDK 21 + Gradle 8.13 + AGP 8.4.2
- minSdk 24, targetSdk 35, compileSdk 35
- 功能：[一句话描述核心逻辑]
- 边界：[不影响/不涉及的内容]
- 图标：[描述或指定文件]
- 构建：assembleDebug（可直接安装）
```

### 崩溃排查模板

```
现象：[一句话描述]
日志：
> adb logcat -s [TAG]:*
[粘贴日志]
```

### 增量修改模板

```
[动作] [路径或文件] [约束条件]
例：复制 app-debug.apk 到 bak/，文件名加时间戳
例：将图标换为 C:\xx.png，所有引用位置同步更新
```

---

## 四、本次开发统计

```
总轮次       ~30
有效编码     ~15（50%）
环境修复     ~10（33%）
崩溃排查     ~4（13%）
文档         ~1（3%）
```

**结论：** 环境兼容性占了三分之一以上的开销。下次类似项目，
**在提示词中锁定 JDK/Gradle/SDK 版本能节省大量迭代时间**。
